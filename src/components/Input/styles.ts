import styled from 'styled-components';

export const Container = styled.div`
  input {
    width: 21.5rem;
    height: 3rem;
    border-color: #1d164d 1px;

    font-family: 'Mulish', sans-serif;
    border-radius: 5px;

    ::placeholder {
      color: #b4b4b4;
    }
  }
`;
